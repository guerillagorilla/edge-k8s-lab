apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-hello.fullname" . }}
  labels:
    {{- include "edge-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edge-hello.selectorLabels" . | nindent 4 }}
